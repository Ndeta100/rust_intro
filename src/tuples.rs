//Tuples are grouped together values of different types and//max is 12 elements

pub fn run(){
let person :(&str, &str, i8)=("Ndeta","Mars",37);
println!("{} is from {} and is {}",person.0,person.1,person.2);
}